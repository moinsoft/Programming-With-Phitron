#include<stdio.h>
#include<string.h>
#include<math.h>
#include<stdlib.h>

int main() {

    /* Take two non-negative integer X and Y as input and output the multiplication of those two numbers. */

    int a,b,mul;

    scanf("%d %d",&a,&b);

    mul = a*b;

    printf("%d",mul);

    return 0;
}

